---
import type { NavItem } from '../types';

const navItems: NavItem[] = [
  {
    name: 'About',
    to: '/about',
  },
  {
    name: 'Blog',
    to: '/blog',
  },
  {
    name: 'Shop',
    to: '/shop',
  },
];
---

<header class="flex justify-between items-center px-4 h-16">
  <a href="/">
    <h1 class="font-bold text-4xl text-primary flex items-end gap-2">
      🌭 AstroWich
    </h1>
  </a>
  <nav class="flex items-center gap-4">
    {
      navItems.map((item) => (
        <a
          href={item.to}
          class="hover:underline hover:text-primary transition-all duration-300 ease-linear"
        >
          {item.name}
        </a>
      ))
    }
  </nav>
</header>
